# subrepos — cloned separately; releases pinned in components.toml
/kak/
/kak-lsp/
/kak-tree-sitter/

# all build outputs — binaries, grammars, intermediate artifacts
/dist/

# compiled ok binary — produced by mise run build-ok (jpm output)
/bin/

# jpm build artifacts
/build/

# jj internal state (manages its own git-backend tracking)
/.jj/
