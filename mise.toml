[tools]
zig = "0.16.0"
rust = "stable"
"cargo:cargo-zigbuild" = "latest"
# janet is not in the mise registry — install via system package manager:
#   fedora: sudo dnf install janet
#   macos:  brew install janet
# Used to develop and compile src/ (the ok binary).
